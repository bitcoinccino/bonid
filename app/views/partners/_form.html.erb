<%# app/views/partners/_form.html.erb %>
<%# Partner signup form - Single page with dark blue background %>
<%# LEFT: Benefits, RIGHT: White form card %>

<div class="partner-page-wrapper">
  <%# Note: data-controller="sector" and JSON scripts are inside _unified_partner_form %>

  <div class="partner-container">
    <div class="partner-grid">

      <!-- LEFT COLUMN: Benefits -->
      <%= render "partners/form_left", f: f %>

      <!-- RIGHT COLUMN: Form -->
      <%= render "partners/form_right", f: f %>

    </div>
  </div>
</div>
