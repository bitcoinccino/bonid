You’ve been invited to join <%= @partner.name %> as a Partner Admin on BonID.

Click the link below to accept your invitation:

<%= @invite_url %>
<% if @oauth_secret_url.present? %>

== OAuth API Credentials ==
Your credentials are available via a secure one-time link (expires in 72 hours):

  <%= @oauth_secret_url %>

IMPORTANT: This link can only be viewed once. Save the credentials immediately.
<% end %>

If you did not request this invitation, you can safely ignore this email.

— BonID Team
