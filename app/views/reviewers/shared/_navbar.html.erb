<nav class="navbar navbar-expand-lg bg-white shadow-sm fixed-top px-3 py-2">
  <div class="container-fluid d-flex justify-content-between align-items-center">

    <!-- Branding -->
    <%= link_to reviewers_root_path, class: "fw-bold text-haitian-blue font-orbitron fs-5 text-decoration-none" do %>
      <i class="ri-shield-check-line me-2"></i>BonID Reviewer
    <% end %>

    <!-- User Menu -->
    <div class="dropdown">
      <a class="d-flex align-items-center text-decoration-none" href="#" 
         role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="ri-user-line me-2"></i>
        <span><%= current_reviewer&.full_name || "Reviewer" %></span>
        <i class="ri-arrow-down-s-line ms-1"></i>
      </a>

      <ul class="dropdown-menu dropdown-menu-end shadow-sm">
        <li class="dropdown-header small text-muted">
          Signed in as <strong><%= current_reviewer&.email %></strong>
        </li>

        <li><hr class="dropdown-divider"></li>

        <li><%= link_to "Profile", "#", class: "dropdown-item" %></li>

        <li>
          <%= link_to "Logout",
              destroy_reviewer_session_path,
              method: :delete,
              class: "dropdown-item text-danger" %>
        </li>
      </ul>
    </div>
  </div>
</nav>
