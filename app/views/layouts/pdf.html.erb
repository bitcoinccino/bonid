<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title) || "BonID Incident Report" %></title>
    <style>
      /* Basic Bootstrap-style layout without CDN dependency */
      body {
        font-family: "Helvetica", "Arial", sans-serif;
        font-size: 14px;
        line-height: 1.6;
        color: #333;
        margin: 20px;
      }

      h1, h2, h3 {
        font-weight: bold;
        margin-bottom: 10px;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
      }

      th, td {
        border: 1px solid #ddd;
        padding: 8px;
      }

      th {
        background-color: #f8f8f8;
        font-weight: bold;
      }

      /* Hide UI elements that shouldn't appear in PDF */
      .no-print {
        display: none !important;
      }

      .section {
        margin-bottom: 30px;
      }

      .section-title {
        font-size: 18px;
        margin-bottom: 12px;
        border-bottom: 2px solid #000;
        padding-bottom: 4px;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
