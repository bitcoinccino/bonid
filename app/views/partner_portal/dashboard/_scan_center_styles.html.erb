<style>
  .dgi-alerts { display: flex; flex-direction: column; gap: 8px; margin-bottom: 20px; }
  .dgi-alert-item {
    display: flex; align-items: center; gap: 12px;
    padding: 10px 16px; border-radius: 10px;
    font-size: 0.82rem; font-weight: 500; color: #0F172A;
  }
  .dgi-alert-item--review { background: #FEF3C7; border-left: 4px solid #D97706; }
  .dgi-alert-item--review .dgi-alert-icon { color: #D97706; }
  .dgi-alert-item--payment { background: #DBEAFE; border-left: 4px solid #2563EB; }
  .dgi-alert-item--payment .dgi-alert-icon { color: #2563EB; }
  .dgi-alert-item--danger { background: #FEE2E2; border-left: 4px solid #DC2626; }
  .dgi-alert-item--danger .dgi-alert-icon { color: #DC2626; }
  .dgi-alert-icon { font-size: 1.1rem; flex-shrink: 0; }
  .dgi-alert-body { flex: 1; line-height: 1.3; }
  .dgi-alert-action {
    padding: 5px 14px; font-weight: 700; font-size: 0.72rem;
    color: #0d132d; background: rgba(255,255,255,0.8); border-radius: 6px;
    text-decoration: none; white-space: nowrap;
  }
  .dgi-alert-action:hover { background: #FFF; color: #0d132d; text-decoration: none; }

  .dgi-scan-center { margin-bottom: 24px; }
  .dgi-scan-card {
    background: linear-gradient(135deg, #0d132d 0%, #1a2340 50%, #0d132d 100%);
    border-radius: 20px; padding: 40px 32px;
    text-align: center; position: relative; overflow: hidden;
  }
  .dgi-scan-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
    background-size: 40px 40px; pointer-events: none;
  }
  .dgi-scan-visual { position: relative; z-index: 1; max-width: 400px; margin: 0 auto; }
  .dgi-scan-ring {
    width: 100px; height: 100px; border-radius: 50%;
    border: 2px solid rgba(255,255,255,0.15);
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 20px; animation: dgi-ring-pulse 3s ease-in-out infinite;
  }
  .dgi-scan-ring-inner {
    width: 72px; height: 72px; border-radius: 50%;
    background: rgba(255,255,255,0.1);
    display: flex; align-items: center; justify-content: center;
    border: 2px solid rgba(255,255,255,0.2);
  }
  .dgi-scan-ring-inner i { font-size: 2rem; color: #FFF; }
  @keyframes dgi-ring-pulse {
    0%, 100% { border-color: rgba(255,255,255,0.15); transform: scale(1); }
    50% { border-color: rgba(255,255,255,0.3); transform: scale(1.05); }
  }
  .dgi-scan-title { font-weight: 800; font-size: 1.4rem; color: #FFFFFF !important; margin: 0 0 6px; }
  .dgi-scan-desc { font-size: 0.82rem; color: rgba(255,255,255,0.6); margin: 0 0 24px; line-height: 1.4; }
  .dgi-scan-form { margin: 0 auto; max-width: 320px; }
  .dgi-scan-input-wrap {
    display: flex; background: rgba(255,255,255,0.12);
    border: 2px solid rgba(255,255,255,0.25); border-radius: 14px; overflow: hidden;
  }
  .dgi-scan-input-wrap:focus-within { border-color: rgba(255,255,255,0.6); background: rgba(255,255,255,0.18); }
  .dgi-scan-input {
    flex: 1; padding: 14px 18px; background: transparent; border: none; color: #FFF;
    font-family: 'JetBrains Mono', 'Courier New', monospace;
    font-size: 1.1rem; font-weight: 600; letter-spacing: 3px;
    text-transform: uppercase; text-align: center;
  }
  .dgi-scan-input::placeholder { color: rgba(255,255,255,0.35); letter-spacing: 2px; font-size: 0.9rem; }
  .dgi-scan-input:focus { outline: none; }
  .dgi-scan-submit {
    padding: 14px 18px; background: #FFF; border: none; color: #0d132d;
    font-size: 1.1rem; cursor: pointer; display: flex; align-items: center;
  }
  .dgi-scan-submit:hover { background: #EFF6FF; }
  .dgi-scan-divider { display: flex; align-items: center; gap: 16px; margin: 20px 0; }
  .dgi-scan-divider::before, .dgi-scan-divider::after {
    content: ''; flex: 1; height: 1px; background: rgba(255,255,255,0.15);
  }
  .dgi-scan-divider span {
    font-size: 0.72rem; font-weight: 500; color: rgba(255,255,255,0.4);
    text-transform: uppercase; letter-spacing: 0.1em;
  }
  .dgi-scan-qr-btn {
    display: inline-flex; align-items: center; padding: 12px 28px;
    background: transparent; color: #FFF; font-weight: 600; font-size: 0.85rem;
    border: 2px solid rgba(255,255,255,0.3); border-radius: 12px; text-decoration: none;
  }
  .dgi-scan-qr-btn:hover {
    background: rgba(255,255,255,0.1); border-color: rgba(255,255,255,0.5);
    color: #FFF; text-decoration: none;
  }
  .dgi-metrics {
    display: grid; grid-template-columns: repeat(4, 1fr); gap: 14px; margin-bottom: 24px;
  }
  .dgi-metric-card {
    display: flex; align-items: center; gap: 14px; padding: 18px 16px;
    background: #FFF; border: 1px solid #E2E8F0; border-radius: 14px;
  }
  .dgi-metric-card:hover { border-color: #CBD5E1; box-shadow: 0 2px 8px rgba(0,0,0,0.04); }
  .dgi-metric-icon {
    width: 44px; height: 44px; border-radius: 12px;
    display: flex; align-items: center; justify-content: center; flex-shrink: 0;
  }
  .dgi-metric-icon i { font-size: 1.2rem; }
  .dgi-metric-icon--blue { background: #EFF6FF; color: #2563EB; }
  .dgi-metric-icon--blue i { color: #2563EB; }
  .dgi-metric-icon--green { background: #ECFDF5; color: #059669; }
  .dgi-metric-icon--green i { color: #059669; }
  .dgi-metric-icon--amber { background: #FFFBEB; color: #D97706; }
  .dgi-metric-icon--amber i { color: #D97706; }
  .dgi-metric-icon--purple { background: #F5F3FF; color: #7C3AED; }
  .dgi-metric-icon--purple i { color: #7C3AED; }
  .dgi-metric-data { min-width: 0; }
  .dgi-metric-num { display: block; font-weight: 800; font-size: 1.35rem; color: #0F172A; line-height: 1.2; }
  .dgi-metric-label {
    display: block; font-size: 0.68rem; font-weight: 500; color: #94A3B8;
    text-transform: uppercase; letter-spacing: 0.03em; margin-top: 1px;
  }
  @media (max-width: 768px) {
    .dgi-scan-card { padding: 28px 20px; }
    .dgi-scan-ring { width: 80px; height: 80px; }
    .dgi-scan-ring-inner { width: 56px; height: 56px; }
    .dgi-scan-ring-inner i { font-size: 1.5rem; }
    .dgi-scan-title { font-size: 1.15rem; }
    .dgi-metrics { grid-template-columns: repeat(2, 1fr); gap: 10px; }
    .dgi-metric-card { padding: 14px 12px; gap: 10px; }
    .dgi-metric-icon { width: 38px; height: 38px; }
    .dgi-metric-num { font-size: 1.1rem; }
  }
  @media (max-width: 480px) {
    .dgi-alerts { gap: 6px; }
    .dgi-alert-item { padding: 8px 12px; font-size: 0.75rem; }
    .dgi-scan-input { font-size: 0.95rem; padding: 12px 14px; }
  }
</style>
