<div class="container-fluid py-4">
  <div class="d-flex align-items-center justify-content-between mb-4">
    <div>
      <h4 class="pp-page-title">
        <i class="ri-edit-2-line me-2 pp-text-brand"></i>Modifye BV #<%= @station.bv_number %>
      </h4>
      <p class="pp-page-subtitle"><%= @center.name %></p>
    </div>
    <%= link_to "← Tounen", partner_portal_polling_center_path(@center), class: "btn btn-link" %>
  </div>

  <div class="pp-card card">
    <div class="card-body p-4">
      <%= render "form" %>
    </div>
  </div>
</div>
